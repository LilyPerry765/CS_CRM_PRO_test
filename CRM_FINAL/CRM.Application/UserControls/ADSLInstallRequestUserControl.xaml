<UserControl x:Class="CRM.Application.UserControls.ADSLInstallRequestUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel FlowDirection="RightToLeft">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--<Label Grid.Row="0" Content="اولویت مشترک : " />
                    <ComboBox Grid.Row="0" Grid.Column="1" Name="CustomerPriorityComboBox"  DisplayMemberPath="Name" SelectedValuePath="ID" />-->

            <Label Grid.Row="0" Grid.Column="0" Content="نصب و راه اندازی توسط مخابرات : " />
            <CheckBox Grid.Row="0" Grid.Column="1" Name="RequiredInstalationCheckBox" VerticalAlignment="Center" IsChecked="True" IsEnabled="False" />

        </Grid>
    </DockPanel>
</UserControl>
