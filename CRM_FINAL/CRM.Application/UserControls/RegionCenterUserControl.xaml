<UserControl x:Class="CRM.Application.UserControls.RegionCenterUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid FlowDirection="RightToLeft">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="شهر : " VerticalAlignment="Center" Width="100"/>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="RegionComboBox" DisplayMemberPath="Title" SelectedValuePath="ID" SelectionChanged="RegionComboBox_SelectionChanged"/>
        
        <TextBlock Grid.Row="0" Grid.Column="3" Text="مرکز : " VerticalAlignment="Center" Width="100" HorizontalAlignment="Left" Margin="0,4,0,5"/>
        <ComboBox Grid.Row="0" Grid.Column="4" x:Name="CenterComboBox" DisplayMemberPath="CenterName" SelectedValuePath="ID"/>
    </Grid>
</UserControl>
