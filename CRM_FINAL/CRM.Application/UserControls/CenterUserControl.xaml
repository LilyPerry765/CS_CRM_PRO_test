<UserControl x:Class="CRM.Application.UserControls.CenterUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
               xmlns:UserControls="clr-namespace:CRM.Application.UserControls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <UserControls:CheckableComboBox Grid.Row="0" x:Name="CityComboBox" LostFocus="CityComboBox_LostFocus"/>

        <UserControls:CheckableComboBox  Grid.Row="1" x:Name="CenterComboBox" LostFocus="CenterComboBox_LostFocus"/>

    </Grid>
</UserControl>
